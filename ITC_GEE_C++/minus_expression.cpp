#include <iostream>
using namespace std;

int main()
{
    // Declaration 
    int x = 4;
    int y = -10;
    // Applying plus and minus
    cout << "Using plus operator on x:" << +x << endl;
    cout << "Using minus operator on x:" << -x << endl;
    // Applying plys and minus
    cout << "Using plus operator on y:" << +y << endl;
    cout << "Using minus operator on y:" << -y << endl;
    return 0;
}